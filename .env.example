# Exampole file. Real one should never be commited
ANTHROPIC_API_KEY=your-key-here
DATABASE_URL=postgresql://triage:triage_pass@localhost:5432/transaction_triage
NIGHT_AGENT_CUTOFF_HOURS=72
SEED_RANDOM_SEED=17
# Optional tuning — defaults works fine
# MODEL_NAME=claude-sonnet-4-6
# MAX_RETRIES=5
# MCP_HTTP_TIMEOUT=30.0
# Agent tuning — increase for production
# Tune these for your API tier
# MAX_TOKENS=16000
# MAX_ITERATIONS=500
# DAY_AGENT_BATCH_SIZE=50
